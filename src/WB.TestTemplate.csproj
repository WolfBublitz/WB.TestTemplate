<Project Sdk="Microsoft.NET.Sdk">

  <!-- Project Info -->
  <PropertyGroup>
    <Title>WB Test Templates</Title>
    <Authors>Wolf Bublitz</Authors>
    <Description>Template for a MSTest project.</Description>
    <RepositoryUrl>https://github.com/WolfBublitz/WB.TestUtils</RepositoryUrl>
  </PropertyGroup>

  <!-- Version -->
  <PropertyGroup>
    <MajorVersion>1</MajorVersion>
    <MinorVersion>1</MinorVersion>
    <PatchVersion>0</PatchVersion>
  </PropertyGroup>

  <!-- Package -->
  <PropertyGroup>
    <PackageType>Template</PackageType>
    <PackageId>WB.TestTemplate</PackageId>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageLicenseFile>LICENSE.md</PackageLicenseFile>
    <PackageTags>dotnet-new;templates;wb;testing</PackageTags>
    <PackageReleaseNotes>https://github.com/WolfBublitz/WB.TestTemplate/blob/master/$(MajorVersion).$(MinorVersion).x/CHANGELOG.md#$(MajorVersion)$(MinorVersion)$(PatchVersion)</PackageReleaseNotes>
  </PropertyGroup>

  <!-- Additional Properties -->
  <PropertyGroup>
    <Version>$(MajorVersion).$(MinorVersion).$(PatchVersion)</Version>
    <TargetFramework>net8.0</TargetFramework>
    <IncludeContentInPack>true</IncludeContentInPack>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <ContentTargetFolders>content</ContentTargetFolders>
  </PropertyGroup>

  <!-- Warnings -->
  <PropertyGroup>
    <NoWarn>$(NoWarn);NU5128</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="templates\**\*" Exclude="templates\**\bin\**;templates\**\obj\**" />
    <Compile Remove="**\*" />
    <None Include="../README.md" Pack="true" PackagePath="\"/>
    <None Include="../LICENSE.md" Pack="true" PackagePath="\"/>
  </ItemGroup>

</Project>